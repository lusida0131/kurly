<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.kurly.api.ApiMapper">
    <insert id="apiSave">
        insert into product (no, name, basePrice, allergy, shortDescription, salesUnit, volume, deliveryTypeNames, sellerName )
        values ( #{no}, #{name}, #{basePrice}, #{allergy}, #{shortDescription}, #{salesUnit}, #{volume}, #{deliveryTypeNames}, #{sellerName} )
    </insert>
</mapper>
